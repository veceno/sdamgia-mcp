mcp[cli]>=1.0.0
# sdamgia-api depends on 'grabzit' which may fail to build on some systems.
# If 'pip install -r requirements.txt' fails with a grabzit build error, run:
#   pip install --no-deps sdamgia-api
#   pip install -r requirements.txt
sdamgia-api>=0.1.7
httpx>=0.24.0
python-dotenv>=1.0.0
